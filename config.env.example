# Archivo de configuración para ixxxi.sh
# Copia este archivo a .hadixxity.env y ajusta los valores.
# El script cargará automáticamente este archivo si está presente.

# --- Shodan CLI ---
# Requerido cuando se usa -S / --shodan
SHODAN_API_KEY="your-shodan-api-key"

# --- SpiderFoot HX ---
# Usado cuando se habilita -X / --spiderfoot para generar planes o hooks de automatización futuros.
SPIDERFOOT_URL="https://spiderfoot.example.com"
SPIDERFOOT_API_KEY="your-spiderfoot-api-key"

# --- ProjectDiscovery Cloud ---
# Usado por pipelines subfinder/httpx (apex automático + manual -A). 
# Obtén una clave en https://cloud.projectdiscovery.io/
PROJECTDISCOVERY_API_KEY=""

# --- SecurityTrails ---
# Útil para lookups DNS/historial si extiendes el script con su API.
SECURITYTRAILS_API_KEY="your-securitytrails-api-key"

# --- Censys ---
# Descomenta si/ cuando agregues endpoints de Censys al workflow.
CENSYS_API_ID="your-censys-api-id"
CENSYS_API_SECRET="your-censys-api-secret"

# --- Otros proveedores ---
# Agrega más aquí (SecurityTrails WHOIS history, SecurityScorecard, etc.) 
# a medida que expandas módulos.

